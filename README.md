Arduino script to read two RLS RM08 encoders and adjust the Analog voltage on the DAC to show these values. You can alos calibrate the encoders in the zero position by pressing a 'caliberation' button